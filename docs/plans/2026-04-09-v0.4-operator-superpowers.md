# v0.4 "Operator Superpowers" — Lean Plan

**Goal:** Make the tool useful beyond just sending — content snapshots, dry-run, UTM links, revenue tracking, CSV export. Ship the features that differentiate this from "a binary that sends emails."

**Migration 0006** = content_snapshots_and_revenue (broadcast snapshot columns + revenue table).

**Priority order (highest impact first):**

1. Content snapshot at send time (BG-1) — compliance foundation
2. `broadcast send --dry-run` (P0.4) — operator safety net
3. UTM link auto-injection (MON-1) — attribution foundation  
4. Stripe client_reference_id injection (MON-2) — pairs with MON-1
5. Revenue table + `revenue add` + `revenue import` + `report revenue` (MON-3)
6. `--format csv` on report commands (P0.5)
7. Affiliate link wrapping (MON-4)
8. Surface revenue in `report show` (MON-5)
9. `report ltv` (MON-6)
10. Subject spam preview (P0.3)
11. `contact sunset` (P1.5)
12. `bounce show` (P1.3)
13. Idempotent `broadcast create` (P0.2)
14. README update (P0.1)
15. Release (bump, smoke, tag, push, crates, homebrew)

**Not in v0.4:** dnscheck (adds a dep, not worth it for a patch), daemon, subscription forms, drip sequences, audit log, db split.
